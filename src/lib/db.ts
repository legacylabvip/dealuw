// Database stub — SQLite removed for Vercel deployment
// TODO: Replace with Vercel Postgres when adding persistent storage

export default function getDb(): never {
  throw new Error('Database not available in serverless environment');
}
